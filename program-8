//to write a program to find all the odd numbers in a limit
const prompt = require("prompt-sync")();
var limit = prompt("enter the limit ");
var sum = 0;
for(i=0;i<=limit;i++){
    if(i%2 !=0){
        sum =sum+i;    }
}
console.log("sum is "+sum);
